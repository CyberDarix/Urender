<Window x:Class="URender.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="urender"
        Width="980"
        Height="520"
        MinWidth="820"
        MinHeight="420"
        Background="#E4E6EA"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="True">
    <Window.Resources>
        <BitmapImage x:Key="AppLogo" UriSource="/Assets/urender-logo.png" />
        <SolidColorBrush x:Key="TextBrush" Color="#25262A" />
        <SolidColorBrush x:Key="MutedBrush" Color="#8B8E96" />
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Style x:Key="ConvertButton" TargetType="Button">
            <Setter Property="Background" Value="#FF3038" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4A51" />
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.015" Duration="0:0:0.16" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.015" Duration="0:0:0.16" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.16" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.16" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D9232A" />
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="FormatCombo" TargetType="ComboBox">
            <Setter Property="Width" Value="78" />
            <Setter Property="Height" Value="34" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#34363B" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#C9CBD0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="MaxDropDownHeight" Value="320" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="180" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="#F5F5F5" />
                        <Setter Property="Background" Value="#242426" />
                        <Setter Property="Width" Value="52" />
                        <Setter Property="Height" Value="34" />
                        <Setter Property="Margin" Value="3" />
                        <Setter Property="Padding" Value="3" />
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Style.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#FF3038" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Border Background="#F7F8FA"
                CornerRadius="2"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                MaxWidth="982"
                Width="982">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.14" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="62" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Image x:Name="EmptyLogo"
                           Source="{StaticResource AppLogo}"
                           Width="74"
                           Height="74"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0,26" />
                    <StackPanel x:Name="FileRows" />
                </Grid>

                <Grid Grid.Row="1" Background="#292A2C">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="240" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="240" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0"
                            Width="202"
                            Height="34"
                            HorizontalAlignment="Center"
                            Background="#FAFAFA"
                            CornerRadius="2">
                        <Button Style="{StaticResource FlatButton}" Click="ChooseFiles_Click">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="+" Foreground="#FF3038" FontSize="18" FontWeight="Normal" Margin="0,0,14,1" VerticalAlignment="Center" />
                                <TextBlock Text="Ajouter plus de fichiers" Foreground="#282A2E" FontSize="12" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </Border>
                    <Border Grid.Column="1" />
                    <Border Grid.Column="2">
                        <Button Style="{StaticResource ConvertButton}" Click="Convert_Click">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Convertir"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="→"
                                           Foreground="White"
                                           FontSize="29"
                                           Margin="24,0,0,2"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
